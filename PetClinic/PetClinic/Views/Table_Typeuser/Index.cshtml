@model IEnumerable<PetClinic.Models.Table_Typeuser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeuserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeuserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Typeuser_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Typeuser_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Typeuser_ID })
        </td>
    </tr>
}

</table>
